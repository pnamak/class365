# Local override — host port + ensure a local `coolify` network exists:
#   docker network create coolify 2>/dev/null || true
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up --build
services:
  app:
    ports:
      - "8080:80"
